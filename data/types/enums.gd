extends Resource

class_name Enums

enum GameMode {
	VILLAGE,
	VILLAGE_EVENT,
	SCAVENGE,
	SURVIVAL,
}

enum Gender {
	MALE,
	FEMALE,
}

enum HeroTier {
	S,
	A,
	B,
}

enum HeroType {
	FIGHTER,
	ASSASIN,
	MAGE,
}
